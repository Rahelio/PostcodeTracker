# Example environment variables
# Copy this file to .env for local development
# Do not commit .env with real values to version control

# Flask configuration
SESSION_SECRET=sNbmFT7MYzKz

# Database configuration (optional - will use SQLite if not set)
# For PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# For SQLite (this will be used by default if DATABASE_URL is not set):
# DATABASE_URL=sqlite:///postcode_distances.db